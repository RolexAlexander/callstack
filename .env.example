# Copy to .env and fill in real values. Never commit .env.
GOOGLE_API_KEY=
CALLE_API_KEY=

# When set to 1, the CALL-E call is stubbed (no real call, no spend) so the
# diagnosis pipeline can be verified for free before spending call credits.
SUPPORT_MOCK=0
